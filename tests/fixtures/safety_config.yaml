# Safety config used in tests/conftest.py and integration tests.
# Reviewed by construction: these are a fixture's limits, not a rig's.
schema_version: 2
reviewed: true
rig_profile:
  mode: guaranteed
  categorical_properties: []
  excluded_properties: []
stage:
  x_min: -1000.0
  x_max:  1000.0
  y_min: -1000.0
  y_max:  1000.0
  z_min:  0.0
  z_max:  200.0

camera:
  max_exposure_ms: 2000.0
